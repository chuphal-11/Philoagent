[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "philoagent"
version = "0.1.0"
description = "Philosopher AI Agent built with LangGraph"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]